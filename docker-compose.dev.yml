services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: snappy-server-dev
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run start:dev
