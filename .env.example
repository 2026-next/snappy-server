# Runtime DB URL (same VM PostgreSQL)
# Use this in application runtime when the app and DB run on one VM.
# Example:
# DATABASE_URL="postgresql://app_user:YOUR_PASSWORD@localhost:5432/snappy"
DATABASE_URL="postgresql://app_user:YOUR_PASSWORD@localhost:5432/snappy"

# Direct DB URL for Prisma CLI tasks (migrate/introspect)
# In same-VM setups this is usually identical to DATABASE_URL.
DIRECT_URL="postgresql://app_user:YOUR_PASSWORD@localhost:5432/snappy"

JWT_ACCESS_SECRET="your-access-secret"
JWT_REFRESH_SECRET="your-refresh-secret"
JWT_ACCESS_EXPIRES_IN_SECONDS=900 # 15 minutes
JWT_REFRESH_EXPIRES_IN_SECONDS=604800 # 7 days

# OAuth providers
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
GOOGLE_CALLBACK_URL="http://localhost:3000/auth/google/callback"
KAKAO_CLIENT_ID="your-kakao-client-id"
KAKAO_CLIENT_SECRET="your-kakao-client-secret"